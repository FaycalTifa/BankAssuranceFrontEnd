import { FormatMontantDirective } from './format-montant.directive';

describe('FormatMontantDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatMontantDirective();
    expect(directive).toBeTruthy();
  });
});
