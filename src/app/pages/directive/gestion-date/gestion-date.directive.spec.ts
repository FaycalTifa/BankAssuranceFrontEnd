import { GestionDateDirective } from './gestion-date.directive';

describe('GestionDateDirective', () => {
  it('should create an instance', () => {
    const directive = new GestionDateDirective();
    expect(directive).toBeTruthy();
  });
});
